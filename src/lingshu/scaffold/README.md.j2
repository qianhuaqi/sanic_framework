# {{ project_name }}

Generated Sanic MVC API project.

## Run

```powershell
copy .env.example .env
python -m pip install -e ".[dev]"
python run.py
```

Run from the project root so `config/defaults.py`, `app/`, and `public/` are discovered correctly.

For PyCharm, set the interpreter to the project's virtual environment and set the working directory to the project root.

## Add Version

```powershell
lingshu add v1
lingshu make module v1 demo
```

## Enabled Databases

{{ databases | join(", ") if databases else "none" }}
